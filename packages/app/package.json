{
  "name": "@recap/app",
  "version": "0.0.1",
  "description": "reCap app",
  "private": true,
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --public-url ./ --no-scope-hoist && cp -R src/static/* dist"
  },
  "dependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "parcel-reporter-static-files-copy": "^1.3.4"
  }
}
