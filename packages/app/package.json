{
  "name": "@recap/app",
  "version": "0.0.1",
  "description": "reCap web app",
  "private": true,
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "start:emulator": "USE_EMULATOR=true parcel",
    "clean": "rimraf dist",
    "prebuild": "yarn clean && yarn workspace @recap/shared build",
    "build": "NODE_ENV=production parcel build --no-content-hash --no-cache --no-source-maps --detailed-report 0"
  },
  "dependencies": {
    "@recap/shared": "0.0.1",
    "react": "^18.2.0",
    "recoil": "^0.7.7",
    "redux": "^4.2.1"
  },
  "devDependencies": {}
}