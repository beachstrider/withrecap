{
  "references": [
    {
      "path": "../shared"
    }
  ],

  "extends": "../../tsconfig.shared.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",

    "jsx": "react",
    "lib": ["dom", "dom.iterable", "esnext"],

    "types": ["@recap/shared", "node"]
  },
  "include": ["src", "../shared/src/hooks"],
  "exclude": ["dist", "node_modules", "config"]
}
