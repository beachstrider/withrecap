{
  "name": "@recap/shared",
  "version": "0.0.1",
  "description": "Shared components",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "source": "src/index.ts",
  "private": true,
  "scripts": {
    "start": "yarn && yarn run -T tsc --watch",
    "build": "yarn && yarn run -T tsc --build"
  },
  "dependencies": {},
  "devDependencies": {}
}
