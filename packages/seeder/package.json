{
  "name": "@recap/seeder",
  "version": "0.0.1",
  "description": "Simple data seeder for Firestore",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "prestart": "yarn clean && yarn workspace @recap/shared build",
    "start": "yarn parcel build src/index.ts",
    "poststart": "node dist/index.js"
  },
  "dependencies": {
    "@recap/shared": "0.0.1"
  },
  "devDependencies": {}
}
