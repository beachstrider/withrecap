{
  "name": "@recap/seeder",
  "description": "Initial data seeder",
  "private": true,
  "scripts": {
    "clean": "npx rimraf dist tsconfig.tsbuildinfo",
    "build": "yarn clean && tsc -b",
    "start": "yarn build && USE_FIREBASE_EMULATORS=false node dist/index.js",
    "start:emulate": "yarn build && USE_FIREBASE_EMULATORS=true node dist/index.js"
  }
}
