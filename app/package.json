{
  "name": "landing-example",
  "version": "0.1.0",
  "private": true,
  "description": "Sample code for landing. Exported with FireJet. Also contains sample code for landing, meeting recap, home, browser chrome dark, browser chrome light, broswer control bar, browser tab with plus, toolbar url controls, add, bubble, star, tick, text, left, gift, search, dots, share, video, meeting brainstorm, google, cursor pointer, button",
  "dependencies": {
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^13.0.0",
    "@testing-library/user-event": "^13.2.1",
    "@types/jest": "^27.0.1",
    "@types/node": "^16.7.13",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.10.0",
    "react-scripts": "5.0.1",
    "typescript": "^4.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npx tailwindcss -i ./styles.css -o ./src/index.css react-scripts build && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
